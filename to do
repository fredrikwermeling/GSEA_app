- allow to click gene set to get more information, including showing which genes that are inlcuded and how they rank in the specific gene set, as well as the LFC/FDR/ranking score in the input list. perhaps this should be a .csv file
- The enrichment graph doesnt look good. could you make it more look how the graph looks when running GSEA scirpt in R. text is overlapping with elements of graph. I also need to be able to move around eg legends, and to be able to change font, font size etc of differnt text.
- make a heatmap of overlaps between the significant genesets. Perhaps you need to cap it for 20? Also, could a condesend version of gene sets can be presented where only genesets with less than x overlap (should be selected), is shown. The removed genesets should be indicate some how (e.g. 1 = also includes... 2 also includess.. )
- also the export svg/png buttons dont work and is made with white text over whtie background. 
- A key issue with these plots is to understand how to interpret the data, what does it mean changing the order in the ranking, what is up and down. could the graph allow for changing the sorting order. Also, could a button be there to show the top 10 and bottom 10 genes, to allow the user to orient themselves? Also, could you add a search button that allows for searching a gene that is indicated in the graph? it should be possible to indicate several genes, eg by pasting a list from excel with gene names in a column format).  also if hovering over the gene set list, could it be exaplained in more detail what does "dutertre estradiol response 24h up" mean?
- differnt enrichment graphs are dfferntly wide. why is this, it hink it makes sense to always have them the same size and also allwo for changing the size with a toggling of x and y axis. 
- There is a lot of space next to the enrichment graph, it could be filled with some inforamtion.
